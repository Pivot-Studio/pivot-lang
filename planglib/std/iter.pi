use core::builtin;

pub trait Iterator<T> {
    fn next() Option<T>;
}

struct name<T> {
    t:T;
}

impl <T> Iterator<T> for name<T> {
    fn next() Option<T> {
        let a: Option<T> = self.t;
        return a;
    }
}



pub fn ret_opt() name<Option<i64>> {
    let b = 1;
    let a:Option<i64> = b;
    let aa:name<Option<i64>> = name{t:a};
    return aa;
}
