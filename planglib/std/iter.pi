use core::builtin;

pub trait Iterator<T> {
    fn next() builtin::Option<T>;
}

struct name<T> {
    t:T;
}

impl <T> Iterator<T> for name<T> {
    fn next() Option<T> {
        return self.t;
    }
}

