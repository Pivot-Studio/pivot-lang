macro test {
    ($($a:@id = $b:@expr,)*) => {
        $(
            io::print_s($a);
            io::printi64ln($b);  
        )*

    };
    ($a:@id) => {
        test!($a = 1,);
    };
}

use std::io;

pub fn test_macros() void {
    let a = "hello";
    test!(a);
    return;
}