struct test {
    a:bool;
    b:f64;
    c:i64;
    d:;
}

fn name(args:test) void {
    args.
    t
    return;
}

struct test1 {
    a:bool;
    b:f64;
    c:i64;
}

use

fn name() void {

    return;
}

fn name1() void {
    let a = 10;
    return;
}

// 此文件中尽量不要改动已有代码，最好在后方添加。改动已有代码可能导致测试不通过

use test::mod;

struct a {
    n:m
    m:mod::
    o:mod::name
}
use test::mod2;
use test::trait1;

fn traitname() void {
    let st = trait1::testSt2{};
    let a:trait1::TestTrait = st;
    let d = st.a;
    a.name();
    trait1::new();
    return;
}

var GLOB = &1;
