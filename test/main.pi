use project1::test::ifel;
use project1::test::generic;
use project1::test::st;
use project1::test::method;
use project1::test::constant;
use project1::test::traits;
use project1::test::simple;
use project1::test::flow;
use project1::test::list;
use pl_test::main;
use core::panic;

fn main() i64 {
    ifel::test_if_else();
    change_global();
    panic::assert(a.a == 100);
    generic::test_generic();
    method::test_impl();
    st::test_struct();
    constant::test_const();
    traits::test_trait();
    simple::test_simple();
    flow::test_flow();
    list::test_list();
    main::simple_test();
    return 0;
}

struct name {
    a: i64;
    b: i64;
}

const a = &name{
    a: 1
};

fn change_global() void {
    a.a = 100;
    return;
}

