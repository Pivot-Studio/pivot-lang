use core::coro::*;
use project1::test::map;
pub fn main() i64 {
    let c = new_coro(||=>{
        for let i = 0; i < 10; i = i + 1 {
            map::test_map();
            println!("coro", i);
            coro__yield();
        }
        return;
    });
    for let i = 0; i<5; i = i + 1 {
        println!("resume", i);
        c.resume();
    }
    c.dispose_coro_stack();
    return 0;
}






